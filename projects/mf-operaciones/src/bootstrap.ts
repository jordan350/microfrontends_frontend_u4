import { bootstrapApplication } from '@angular/platform-browser';
import { OperacionesComponent } from './app/operaciones.component';

bootstrapApplication(OperacionesComponent).catch(error => console.error(error));
