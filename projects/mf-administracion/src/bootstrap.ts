import { bootstrapApplication } from '@angular/platform-browser';
import { AdministracionComponent } from './app/administracion.component';

bootstrapApplication(AdministracionComponent).catch(error => console.error(error));
