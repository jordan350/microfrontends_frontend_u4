import { bootstrapApplication } from '@angular/platform-browser';
import { ReportesComponent } from './app/reportes.component';

bootstrapApplication(ReportesComponent).catch(error => console.error(error));
